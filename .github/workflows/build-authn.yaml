name: Build Authn

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'services/authn/**'
      - '.github/workflows/build-authn.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'services/authn/**'
      - '.github/workflows/build-authn.yml'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '^1.24.6'

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Install dependencies for authn module
      working-directory: ./services/authn
      run: go mod download

    - name: Build authn module
      working-directory: ./services/authn
      run: go build -o build/ ./...